import styled from "styled-components";

const SearchBox = styled.input`
  border-radius: 0.5em;
  width: 100%;
  height: 2em;
`;

const Search = () => {
  return <SearchBox />;
};

export default Search;
